{
  "name": "cucumber-step-builder",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/inline-dist.js",
    "build:raw": "vite build",
  "build:single": "vite build && node scripts/inline-dist.js",
  "build:single:clean": "vite build && node scripts/inline-dist.js --clean-assets",
  "build:single:minimal": "vite build && node scripts/inline-dist.js --no-backup --clean-assets",
  "build:single:datauri": "vite build && node scripts/inline-dist.js --data-uri-js --clean-assets",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
  "react-dom": "^18.2.0",
  "jszip": "^3.10.1",
  "prismjs": "^1.29.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.31",
    "tailwindcss": "^3.3.3",
    "vite": "^4.5.0"
  }
}